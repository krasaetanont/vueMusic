version: "3.8"
services:
  vue-app:
    container_name: vueMusic
    build: .
    ports:
      - "5173:5173"
    volumes:
      - .:/app
      - /app/node_modules
    working_dir: /app
    command: npm run dev -- --host
